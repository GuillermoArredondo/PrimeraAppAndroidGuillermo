<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.primeraappandroidguillermo">

    <!--Añado esta linea para dar permiso al usuario para crear una nota -->
    <uses-permission android:name="com.android.permission.ACTION_CREATE_NOTE" />

    <application
            android:allowBackup="true"
            android:icon="@drawable/cast_ic_mini_controller_play"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".AcercaDeActivity">
        </activity>
        <activity android:name=".NotaActivity">
        </activity>
        <activity android:name=".AmigoActivity">
        </activity>
        <activity android:name=".CorreoActivity">
        </activity>
        <activity android:name=".CitaActivity">
        </activity>
        <activity android:name=".Menu">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>